import { FinalRequestOptions } from "../internal/request-options.js";
import { type LinqAPIV3 } from "../client.js";
import { APIPromise } from "./api-promise.js";
import { type APIResponseProps } from "../internal/parse.js";
export type PageRequestOptions = Pick<FinalRequestOptions, 'query' | 'headers' | 'body' | 'path' | 'method'>;
export declare abstract class AbstractPage<Item> implements AsyncIterable<Item> {
    #private;
    protected options: FinalRequestOptions;
    protected response: Response;
    protected body: unknown;
    constructor(client: LinqAPIV3, response: Response, body: unknown, options: FinalRequestOptions);
    abstract nextPageRequestOptions(): PageRequestOptions | null;
    abstract getPaginatedItems(): Item[];
    hasNextPage(): boolean;
    getNextPage(): Promise<this>;
    iterPages(): AsyncGenerator<this>;
    [Symbol.asyncIterator](): AsyncGenerator<Item>;
}
/**
 * This subclass of Promise will resolve to an instantiated Page once the request completes.
 *
 * It also implements AsyncIterable to allow auto-paginating iteration on an unawaited list call, eg:
 *
 *    for await (const item of client.items.list()) {
 *      console.log(item)
 *    }
 */
export declare class PagePromise<PageClass extends AbstractPage<Item>, Item = ReturnType<PageClass['getPaginatedItems']>[number]> extends APIPromise<PageClass> implements AsyncIterable<Item> {
    constructor(client: LinqAPIV3, request: Promise<APIResponseProps>, Page: new (...args: ConstructorParameters<typeof AbstractPage>) => PageClass);
    /**
     * Allow auto-paginating iteration on an unawaited list call, eg:
     *
     *    for await (const item of client.items.list()) {
     *      console.log(item)
     *    }
     */
    [Symbol.asyncIterator](): AsyncGenerator<Item>;
}
export interface ListChatsPaginationResponse<Item> {
    chats: Array<Item>;
    next_cursor: string;
}
export interface ListChatsPaginationParams {
    cursor?: string;
    limit?: number;
}
export declare class ListChatsPagination<Item> extends AbstractPage<Item> implements ListChatsPaginationResponse<Item> {
    chats: Array<Item>;
    next_cursor: string;
    constructor(client: LinqAPIV3, response: Response, body: ListChatsPaginationResponse<Item>, options: FinalRequestOptions);
    getPaginatedItems(): Item[];
    nextPageRequestOptions(): PageRequestOptions | null;
}
export interface ListMessagesPaginationResponse<Item> {
    messages: Array<Item>;
    next_cursor: string;
}
export interface ListMessagesPaginationParams {
    cursor?: string;
    limit?: number;
}
export declare class ListMessagesPagination<Item> extends AbstractPage<Item> implements ListMessagesPaginationResponse<Item> {
    messages: Array<Item>;
    next_cursor: string;
    constructor(client: LinqAPIV3, response: Response, body: ListMessagesPaginationResponse<Item>, options: FinalRequestOptions);
    getPaginatedItems(): Item[];
    nextPageRequestOptions(): PageRequestOptions | null;
}
//# sourceMappingURL=pagination.d.ts.map